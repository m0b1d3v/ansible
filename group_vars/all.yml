all:

  apt:
    installed:
      - curl
      - dos2unix
      - git
      - git-lfs
      - glances
      - htop
      - jq
      - make
      - rsync
      - screen
      - sqlite3
      - zip
    removed: []
